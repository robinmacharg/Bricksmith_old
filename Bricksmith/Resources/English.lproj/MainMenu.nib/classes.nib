{
    IBClasses = (
        {
            ACTIONS = {
                addCommentClicked = id; 
                addConditionalClicked = id; 
                addLineClicked = id; 
                addMinifigure = id; 
                addModelClicked = id; 
                addPartClicked = id; 
                addQuadrilateralClicked = id; 
                addStepClicked = id; 
                addTriangleClicked = id; 
                advanceOneStep = id; 
                backOneStep = id; 
                duplicate = id; 
                exportSteps = id; 
                gridGranularityMenuChanged = id; 
                hideParts = id; 
                orderFrontMovePanel = id; 
                orderFrontRotationPanel = id; 
                showColors = id; 
                showDimensions = id; 
                showParts = id; 
                showPieceCount = id; 
                snapSelectionToGrid = id; 
                toggleFileContentsDrawer = id; 
                togglePartBrowserDrawer = id; 
                toggleStepDisplay = id; 
                viewingAngleSelected = id; 
                zoomActual = id; 
                zoomIn = id; 
                zoomOut = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                doHelp = id; 
                doPreferences = id; 
                showColors = id; 
                showInspector = id; 
                showMouseTools = id; 
            }; 
            CLASS = LDrawApplication; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}