{
    IBClasses = (
        {
            ACTIONS = {okButtonClicked = id; }; 
            CLASS = DimensionsPanel; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                dimensionsPanel = DimensionsPanel; 
                dimensionsTable = NSTableView; 
                objectController = NSObjectController; 
            }; 
            SUPERCLASS = NSPanel; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}